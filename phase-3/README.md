## Phase 3 – Web UI (Scaffold)

This folder will contain the web UI that:
- Lets the user enter **email + timezone + preferred day/time** to receive the weekly pulse
- Shows a **success screen** after saving schedule
- Allows manual generation/preview of the one-pager and “send now”

Implementation will start after phase 2 backend endpoints are finalized.


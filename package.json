{
  "name": "groww-review-insights-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd phase-3 && npm install && npm run build",
    "dev": "cd phase-3 && npm run dev"
  }
}
